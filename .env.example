# URL del servidor de detección (reemplazar con la URL de tu servidor)
NEXT_PUBLIC_DETECTION_SERVER_URL=http://localhost:5000

# Configuración del servidor (para el backend independiente)
PORT=3000
HOST=0.0.0.0

# Puerto para el servidor de detección
DETECTION_SERVER_PORT=5000

# Variables para entornos no-Vercel (Vercel usa su propio sistema de variables de entorno)
NODE_ENV=production

